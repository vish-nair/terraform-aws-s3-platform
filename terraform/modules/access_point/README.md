# access_point

Creates an S3 access point for an existing bucket. Supports an optional VPC restriction to limit access to traffic originating from a specific VPC, and an optional access point policy.

